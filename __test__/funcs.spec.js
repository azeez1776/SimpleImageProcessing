const { gamma,
    brightness,
    greyscale,
    contrast,
    sepia,
    vibrance,
    saturation,
    hue,
    invert,
    noise,
    fillColor} = require('../index');

describe("testing the simp functions", () => {
    test("testing if are all functions")
})
